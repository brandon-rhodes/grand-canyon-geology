FROM ubuntu:18.04
RUN apt update -y -y
RUN apt upgrade -y -y
RUN apt install -y -y python3-mapnik
RUN apt install -y -y libmapnik-dev
RUN apt install -y -y python-mapnik

# git clone https://github.com/tilemill-project/tilemill.git
# cd tilemill
# nvm install lts/carbon
# nvm use v8.15.0
# npm install
# npm start

CMD cd work && /bin/bash
